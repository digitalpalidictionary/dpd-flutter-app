- 2026-04-06 [POSITIVE] Replacing heavy Material 3 widgets (SegmentedButton) with lightweight custom equivalents gave a dramatic performance win — diagnose the widget tree cost before adding loading hacks
- 2026-04-05 [CONFUSION] During a performance review, skipped verifying the fuzzy_key index and nearly missed that the fuzzy benchmark numbers were credible — always verify the index backing any range query claim
- 2026-04-05 [WORKFLOW] Running /kamma:3-review while plan mode was active blocked fixing findings in-session; review skills need implementation access, not planning mode
- 2026-04-07 [REPEATED] User had to re-state three times that the text field must never be converted except for Velthuis — initial implementations kept adding toRoman() calls that touched the display text
- 2026-04-07 [CONFUSION] Designed listener condition using toRoman() comparison to prevent field overwrite, which broke double-tap sync — the right fix was a _suppressProviderSync flag to distinguish local vs external provider changes
- 2026-04-07 [BEHAVIOR] Removed live Velthuis conversion too eagerly when simplifying — user correctly pointed out Velthuis ASCII sequences don't overlap with any script, so live conversion is safe and should be kept
- 2026-04-07 [POSITIVE] Reviewing multiple external code reviews in one pass and triaging each finding (actionable / not doing / deferred) was efficient and produced a clean, well-justified fix set
- 2026-04-07 [WORKFLOW] The Dart test runner wrapper injected an unsupported `--pause-after-load` flag for this Flutter project, so falling back to direct `flutter test` kept verification moving
- 2026-04-07 [REPEATED] User had to correct the update dialog plan three times — kept designing user-choice (Install/Later) into the dialog instead of just displaying information. Listen for "show" not "ask".
- 2026-04-07 [BEHAVIOR] Gave flutter run instructions to a user who uses justfile — always check for a justfile first and use its recipes instead of raw CLI commands
- 2026-04-08 [WORKFLOW] Race condition between two independent download flows (DB + APK) — gating with ref.listenManual + fireImmediately: true handles both "already ready" and "wait for ready" cases cleanly in one listener
- 2026-04-08 [BEHAVIOR] For behavior-parity fixes, changing adjacent logic exposed unrelated UX regressions; keep the implementation strictly scoped to the requested behavior unless the user explicitly asks for broader cleanup
- 2026-04-09 [BEHAVIOR] Skipped writing kamma thread files to disk before implementing — the /kamma flow requires creating spec.md and plan.md files in kamma/threads/ before starting implementation, even for small changes
- 2026-04-09 [CONFUSION] Set CrossAxisAlignment.start but widget still appeared centered — a min-sized Column inside an Expanded gets vertically centered by default; need Align(topLeft) wrapper to pin it
- 2026-04-09 [POSITIVE] coderabbit review --agent works well as a post-implementation review step
- 2026-04-12 [BEHAVIOR] Used raw `python` in `../dpd-db` and the user had to correct it — this repo uses `uv run` for local project scripts
- 2026-04-12 [WORKFLOW] Review PASSED but a real bug (share intent clobbering search bar display) was only caught when the user tested — round-trip UX paths from external entry points need an explicit review checklist item
- 2026-04-12 [POSITIVE] searchBarTextProvider one-shot pattern (set before searchQueryProvider, cleared in listener) is clean and reusable for any external-trigger display/query split
- 2026-04-12 [BEHAVIOR] GitHub issue search found nothing in spec/plan — silently skipped instead of asking the user; when issue search returns nothing, ask before skipping
- 2026-04-12 [CONFUSION] An external review incorrectly flagged Dart `switch` cases as fall-through; validate review findings against the language semantics and current code before changing anything
- 2026-04-15 [WORKFLOW] Archived a Kamma thread before the user completed manual review — in this repo, stop after local verification, wait for explicit user acceptance, then review/finalize
- 2026-04-16 [WORKFLOW] Before starting a cleanup refactor after review, explicitly confirm whether the user wants strict behavior preservation and keep the refactor inside that boundary
- 2026-04-22 [POSITIVE] Checking the TSV data for exact field values (dpd_sutta_var = 'mūlapaṇṇāsaka 1') caught a DB alias lookup gap mid-implementation that the spec missed — always spot-check representative rows in the source data before finalizing a DB query plan
- 2026-06-05 [BEHAVIOR] First fix for the cold-start search-bar seed mutated a Riverpod provider directly in initState → red screen of death; in this Flutter+Riverpod app, never write to providers during initState/build — defer to addPostFrameCallback (guard with mounted)
- 2026-06-12 [CONFUSION] When auto-selecting a thread for finalization with no name provided, picked the first PASSED thread alphabetically instead of the one with uncommitted changes. Check git status to cross-reference which thread is actually active before finalizing.
- 2026-07-11 [POSITIVE] Fanning out three parallel investigation agents (app dict integration, dpd-db build pipeline, CI/build flags) up front revealed the feature was ~95% a dpd-db task needing zero Flutter code — map the whole pipeline before assuming where the work lives
- 2026-07-11 [WORKFLOW] The `wn` library's built-in downloader timed out mid-stream on the OEWN LMF; fetching with `curl --retry --continue` then loading via `wn.add` was robust — for flaky/large build-time data downloads prefer curl-with-retry + local load over a library's downloader
- 2026-07-11 [BEHAVIOR] Spec claimed CC BY attribution was satisfied by name+author, but the app never renders `dict_meta.author`; review caught it — when adding externally-licensed data, verify attribution is actually VISIBLE in the UI, not just stored in a column
- 2026-07-11 [CONFUSION] Spec pinned OEWN "core edition ~136k words" and assumed a JSON download existed; reality was full oewn:2025 minus capitalized lemmas = 111,198 and XML-only downloads — verify data-source format/counts against the actual source before writing spec numbers
- 2026-07-19 [BEHAVIOR] Verify which theme token actually drives a UI element before proposing a color change — headword uses onSurface (cream), not primary; gray doubles as both label text and outline. The comparison mockup framing was wrong until checked against the real widgets.
- 2026-07-24 [BEHAVIOR] Stopped at a Phase 1 boundary and handed off "for testing" when the early phase produced nothing a user could actually test — user: "wtf are you stopping here?". For a multi-phase feature where early phases aren't independently useful, implement through to a genuinely testable increment before handing back.
- 2026-07-24 [BEHAVIOR] Built an over-elaborate 4-icon debug tile when the user wanted a plain On/Off toggle like every other setting — "why do you always overcomplicate everything". Default to the simplest control that matches existing UI patterns.
- 2026-07-24 [BEHAVIOR] After the user said "finish the thread", jumped straight to a commit message and called it wrapped, skipping the kamma finalize flow (review.md + 4-finalize + archive) — "have you lost the plot?". "Finish/wrap up the thread" means run the finalize flow and archive, not just write a commit message.
- 2026-07-24 [WORKFLOW] Ran two review passes (subagent + coderabbit) but never wrote review.md, so finalize's PASSED gate had nothing to read. Record review.md with the verdict as the last step of reviewing, so finalize can proceed.
- 2026-07-24 [BEHAVIOR] Used code identifiers/jargon in a user-facing status summary; user asked for plain English sentences. Keep user-facing summaries in plain language; save identifiers for commit bodies and docs.
- 2026-07-24 [POSITIVE] Proved the "logo is just Inter-Bold text in a circle" claim with a real inkscape rasterise + PIL pixel-diff (0.71/255) before committing to the code-drawn approach — cheap, decisive evidence for a design-route decision.
- 2026-07-24 [POSITIVE] Running the independent from-scratch subagent review and coderabbit in parallel caught the clipboard crash and the shared-toggle-state issue at no extra wall-clock over one reviewer.
- 2026-07-26 [POSITIVE] The regression test disproved the spec's own edge-case claim — spec said a tap landing exactly on `+` returns `''`, but `start` walks left over non-boundaries while `end` stops at once, so it returns the left half. Writing the test before trusting the spec's stated behaviour caught it, and spec.md was corrected rather than the code.
- 2026-07-26 [BEHAVIOR] Asked the user how to finalize (archive? commit?) when `/kamma:4-finalize` already prescribes archive-then-suggest-commit-message — "read kamma skill dont ask me". Read the command file for the flow instead of prompting for steps the framework already defines.
- 2026-07-27 [BEHAVIOR] Raised an alarm about a tap-to-jump freeze using an unwarmed single-run Flutter widget-test timing (569ms) and a byte-size-based cost ranking; both were wrong once measured properly — first-pump framework warmup dominated the number, and Cone's per-build DOM parse cost 4x more than a larger CPD entry with no HTML processing. User's "let's really reassess the cost" pushback was warranted. Promoted to a global benchmarking rule.
- 2026-07-27 [WORKFLOW] User confirmed manual verification passed in chat, but `plan.md`'s manual-verification checkboxes stayed unticked until `/kamma:3-review` started rather than being updated the moment confirmation arrived — matches the DRIFT GATE rule in workflow.md; update plan.md the instant a task's real-world state changes, not at the next command's start.
- 2026-07-31 [WORKFLOW] Tried `data:` and `file://` URIs via `adb shell am start` to test an Android `BROWSABLE` deep-link intent-filter from Chrome; both failed (Chrome blocks `file://` intents, and remote-shell re-parsing mangled the quoted `data:` URI). A real hosted webpage (local HTTP server + `adb reverse` + genuine `<a href>` tap) was the only reliable way to exercise the actual `BROWSABLE` resolution path — worth reaching for first next time instead of iterating through URI-scheme workarounds.
- 2026-08-21 [BEHAVIOR] Ran `dart format lib/` after making ~60 targeted edits; it reformatted 75 files, most untouched by the thread, and buried the real change in 786 lines of noise. Had to revert every file to pristine and re-apply each edit by scripted replacement. Never run a formatter across a whole directory in a scoped change — format only what you wrote, or nothing.
- 2026-08-21 [BEHAVIOR] A scripted Python rewrite silently converted `lib/screens/entry_screen.dart` from CRLF to LF, turning a 3-line change into a 285-line whole-file diff. Python text mode normalises line endings on read and writes back the platform default. Preserve endings explicitly (`io.open(..., newline='')`) and audit them before reporting a diff.
- 2026-08-21 [BEHAVIOR] Wrote in plan.md that `frequency_table.dart` "needs no change — no niggahīta appears anywhere in it" after skimming its rendering helpers. An independent reviewer found two rendered labels containing the character, disproving the claim with a one-second grep. When declaring a file exempt from a sweep, grep it for the literal thing being swept and paste the result; skimming is not evidence.
- 2026-08-21 [CONFUSION] Added a display-only character conversion without tracing that the app scrapes rendered text back as input (`RenderParagraph.toPlainText()` in tap-to-search). The converted character flowed into the search query and into persisted history, making the setting irreversible for any tapped word. Before treating a transform as "display-only", find every place rendered output is read back as input.
- 2026-08-21 [POSITIVE] Running an independent zero-context review subagent and CodeRabbit in parallel caught a blocking data-corruption bug and a major staleness bug that neither my own review nor the user's eight-step on-device test pass had found. The parallel adversarial pass earned its cost outright.
- 2026-08-21 [CONFUSION] Fixed the main-screen Fuzzy tier's ranking, declared the thread's search-relevance bug fixed, then the user found the dropdown (a separate code path) had the identical bug shape, then found the main-screen's tier ORDERING (Partial rendered above Fuzzy) buried the fix at a third layer. A relevance/ranking defect can recur independently in every UI surface that renders search results from the same underlying data; fixing one surface's ranking is not evidence the others are fixed — each rendering path needs its own explicit before/after measurement against the reported query.
- 2026-08-21 [BEHAVIOR] `test/database/fuzzy_ranking_test.dart`'s first `searchFuzzyExact` assertions used `contains('kamma 1')` rather than checking it was first; this passed even while an alphabetical-sort bug put `kama 1` ahead of `kamma 1` in the real output — only a manual re-measurement (not the test) caught it. When a test exists specifically to verify ranking/order, assert the order itself (first index, `lessThan` on positions), not mere membership — a `contains` check is compatible with any order at all.
- 2026-08-21 [POSITIVE] Before trusting a manual before/after measurement, confirmed the exact database file used by the manual test (`../dpd-db/exporter/share/dpd-mobile.db`) matches what the real app loads at runtime by reading `justfile`'s `push-mobile-db` recipe rather than assuming a discrepancy meant stale test data — ruled out a plausible false lead in one grep instead of guessing.
- 2026-08-21 [REPEATED] User asked three times for plain English and short answers; I kept returning jargon-dense essays with identifiers and file paths in chat. Keep detail in the thread files, not the reply.
- 2026-08-21 [BEHAVIOR] Ran review but stopped before finalize, then started a commit flow on request — user had to point out the thread was never finalized. Finalize is not optional and not implied by "review passed".
- 2026-08-21 [CONFUSION] First solution used a guessed text-scale threshold needing on-device tuning; user's "is this the simplest?" prompted a rethink to a measurement-based Wrap with no constant. A magic number that needs tuning is a sign the real fix has not been found.
- 2026-08-21 [POSITIVE] The independent zero-context reviewer caught a regression I had explicitly claimed was safe (FittedBox killing the header tap target). Adversarial review earned its cost; my own assertion in plan.md was wrong.
- 2026-08-21 [WORKFLOW] CodeRabbit skipped the new file because untracked files are absent from an uncommitted diff. Run `git add -N` on new files before invoking it, or the most review-worthy file goes unreviewed.
- 2026-08-21 [WORKFLOW] CodeRabbit free tier allows 3 reviews per 10 minutes on a repo with no connected org; a re-run to cover a missed file burned the allowance and had to be abandoned.
