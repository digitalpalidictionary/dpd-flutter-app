# Plan: Settings Help Popups

## Phase 1 - Add reusable settings help popup UI
- [x] Create a reusable dialog/widget pair for settings help content.
- [x] Make the popup dismissible by outside tap and back.
- [x] Make the popup scroll when content is longer than the available height.

## Phase 2 - Attach info buttons to each setting row
- [x] Add a consistent title+info pattern to the main settings rows in `lib/widgets/settings_panel.dart`.
- [x] Add the same pattern to per-dictionary visibility rows in `lib/widgets/dict_settings_widget.dart`.
- [x] Write plain-English help text and example states for each setting.

## Phase 3 - Verify and finish thread tracking
- [x] Run focused verification on the changed Flutter files.
- [x] Update the plan to reflect the completed work.

## Finalization note
- [x] User accepted the thread as good enough for now and will manually refine the help copy.
