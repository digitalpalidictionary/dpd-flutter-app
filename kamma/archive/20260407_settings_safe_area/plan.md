# Phase 1: Fix safe area in settings bottom sheet

## Task 1.1 — Add useSafeArea: true to showModalBottomSheet
- [x] In `lib/widgets/settings_panel.dart`, add `useSafeArea: true`
      to the `showModalBottomSheet` call inside `showSettingsOverlay`

## Task 1.2 — Verify
- [x] Confirm build compiles without errors
- [x] Review the change visually
